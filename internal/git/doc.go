// Package git provides Git repository serving and archiving.
//
// The Server type runs a Git HTTP server on the host, allowing
// containers to fetch and push changes. The archive functions
// prepare repository snapshots for container deployment.
package git
